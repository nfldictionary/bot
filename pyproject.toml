[project]
name = "bot"
version = "0.1.0"
description = "Local SIS seasonal data sync and parquet pipeline"
readme = "README.md"
requires-python = ">=3.9"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
